
@{
    ViewBag.Title = "MVC Razor Syntax";


    string urunAdi = "Bilgisayar";
    decimal fiyat = 18000;
    decimal kdv = 1.20M;
    string[] isimler = { "Mustafa", "Orhan", "Serdar", "Mesut", "Alparslan" };
    var tarih = DateTime.Now; // yorum satırı
}

<h2>MVC Razor Syntax</h2>

<h6>Title: @ViewBag.Title</h6>

<div class="row">
    <div class="col-sm-6">
        <img src="https://picsum.photos/500/300" alt="Rasgele resim üretim" class="img-fluid" />
    </div>
    <div class="col-sm-6">
        <h4>Ürün Adı :  @urunAdi</h4>
        <h4>Ürün Fiyatı :  @fiyat ₺</h4>
        <h4>Kdv Dahil :  @(fiyat * kdv) ₺</h4>
    </div>
</div>

<hr />

<h4>Tarih ve saat :  @tarih</h4>

<h4>
    Kısa Tarih :  @tarih.ToShortDateString()
</h4>

@if (tarih.Hour > 18)
{
    <h5>İyi Akşamlar</h5>
}
else if (tarih.Hour > 12 && tarih.Hour < 18)
{
    <h5>İyi Günler</h5>
}
else
{
    <h5>İyi Sabahlar</h5>
}

<h2>Dizideki İsimler</h2>

@foreach (var isim in isimler)
{
    if (isim == isimler[0])
    {
        <b>Hoşgeldin: @isim</b>
    }
    else
    {
        @isim
    }
}

<hr />

Listedeki 2. isim : @isimler[1]
<hr />

@foreach (var isim in isimler)
{
    <h6>@isim</h6>
}

<h3>İsimler Arasına Boşluk</h3>
@foreach (var isim in isimler)
{
    @(isim + " ")
    // isimleri aralarına boşluk bırakarak yazdırır
}

<h3>İsimlerin Önüne Metin Ekleme</h3>
@foreach (var isim in isimler)
{
    @:İsim : @isim
    <hr />
}

<h3>For Örneği</h3>

@for (int i = 0; i < isimler.Length; i++)
{
    <text>İsim : @isimler[i] </text>
    <hr />
}

<h3>For Örneği 2</h3>

@for (int i = 0; i < isimler.Length; i++)
{
    @:İsim : @isimler[i]
    <hr />
}

<h5>
    Kullanıcı Adı: @@aspnet
</h5>

<p>

    kullanıcı adı vb başında @@ işareti olan bir metin kullanmak için çift @@ işareti eklemeliyiz.
</p>

<p>
    info@deneme.com şeklinde at ifadesinden önce yapışık metin varsa bunun c# kodu olmadığını razor syntax anlar.
</p>

<h3>
    Üretim Yılı
</h3>

<select>
    <option value="">Seçiniz</option>
    @for (int i = DateTime.Now.Year; i > 2000; i--)
    {
        <option value="@i">@i</option>
    }
</select>